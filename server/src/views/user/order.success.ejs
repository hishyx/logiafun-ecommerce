<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Success - logiaFun</title>
  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <!-- Google Fonts: Inter -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background-color: #f9fafb;
    }

    .success-card {
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1), 0 8px 10px -6px rgba(0, 0, 0, 0.1);
    }

    .checkmark-animate {
      animation: scaleIn 0.5s ease-out forwards;
    }

    @keyframes scaleIn {
      0% {
        transform: scale(0);
        opacity: 0;
      }

      100% {
        transform: scale(1);
        opacity: 1;
      }
    }
  </style>
</head>

<body class="min-h-screen flex items-center justify-center p-4">

  <div class="max-w-md w-full bg-white rounded-3xl p-8 success-card text-center">
    <!-- Success Icon -->
    <div class="flex justify-center mb-6">
      <div class="w-20 h-20 bg-green-100 rounded-full flex items-center justify-center checkmark-animate">
        <svg class="w-10 h-10 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"></path>
        </svg>
      </div>
    </div>

    <!-- Heading -->
    <h1 class="text-3xl font-bold text-gray-900 mb-2">Thank You!</h1>
    <p class="text-gray-500 mb-6">Your order has been placed successfully.</p>

    <!-- Order ID Details -->
    <div class="bg-gray-50 rounded-2xl py-4 px-6 mb-8 inline-block">
      <p class="text-sm font-medium text-gray-400 uppercase tracking-wider mb-1">Order ID</p>
      <p class="text-xl font-bold text-gray-800">#<%= orderNumber %>
      </p>
    </div>

    <!-- Confirmation Message -->
    <p class="text-sm text-gray-600 mb-8 leading-relaxed">
      A confirmation email has been sent to your registered email address with all the details of your purchase.
    </p>

    <!-- Action Buttons -->
    <div class="space-y-4">
      <a href="/products" class="block w-full bg-black text-white py-4 px-6 rounded-2xl font-semibold hover:bg-gray-800 transition-colors duration-200">
        Continue Shopping
      </a>
      <a href="/user/orders/<%= orderId %>" class="block w-full border-2 border-gray-100 text-gray-700 py-4 px-6 rounded-2xl font-semibold hover:bg-gray-50 transition-colors duration-200">
        View Order Details
      </a>
    </div>
  </div>

</body>

</html>