<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Checkout - logiaFun</title>
    <link rel="stylesheet" href="/css/home.css" />
    <link rel="stylesheet" href="/css/cart.css" />
    <style>
        .checkout-container {
            max-width: 1200px;
            margin: 2rem auto;
            padding: 0 1.5rem;
            display: grid;
            grid-template-columns: 1.5fr 1fr;
            gap: 2rem;
        }

        .checkout-section {
            background: var(--white);
            padding: 2rem;
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-sm);
            margin-bottom: 2rem;
        }

        .checkout-title {
            font-family: "Fredoka", sans-serif;
            font-size: 1.5rem;
            color: var(--primary);
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 0.75rem;
        }

        .checkout-title-text {
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .payment-options {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .payment-option {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 1rem;
            border: 1px solid #eee;
            border-radius: 8px;
            cursor: pointer;
            transition: var(--transition);
        }

        .payment-option:hover {
            border-color: var(--primary);
            background: var(--bg-light);
        }

        .payment-option input {
            cursor: pointer;
        }

        .order-summary-card {
            background: var(--bg-light);
            padding: 2rem;
            border-radius: var(--radius-lg);
            position: sticky;
            top: 100px;
        }

        .summary-item {
            display: flex;
            justify-content: space-between;
            margin-bottom: 1rem;
            color: var(--text-light);
        }

        .summary-total {
            display: flex;
            justify-content: space-between;
            margin-top: 1.5rem;
            padding-top: 1.5rem;
            border-top: 1px solid #ddd;
            font-weight: 700;
            font-size: 1.25rem;
            color: var(--primary);
        }

        .place-order-btn {
            width: 100%;
            margin-top: 2rem;
            padding: 1rem;
            font-size: 1.1rem;
        }

        /* Address Selection Styles */
        .address-selection-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 1rem;
        }

        .address-select-card {
            border: 2px solid #eee;
            border-radius: 12px;
            padding: 1.25rem;
            cursor: pointer;
            transition: var(--transition);
            position: relative;
            display: flex;
            align-items: flex-start;
            gap: 1rem;
        }

        .address-select-card:hover {
            border-color: var(--secondary);
            background: var(--bg-light);
        }

        .address-select-card.selected {
            border-color: var(--primary);
            background: rgba(30, 58, 95, 0.03);
        }

        .address-select-card input[type="radio"] {
            margin-top: 0.25rem;
            cursor: pointer;
        }

        .address-badge {
            font-size: 0.75rem;
            background: var(--bg-light);
            padding: 2px 8px;
            border-radius: 4px;
            font-weight: 600;
            margin-bottom: 0.5rem;
            display: inline-block;
        }

        .address-info {
            flex: 1;
        }

        .address-info strong {
            display: block;
            margin-bottom: 0.25rem;
            font-size: 1.1rem;
        }

        .address-info p {
            margin: 0;
            color: var(--text-light);
            font-size: 0.95rem;
            line-height: 1.4;
        }

        .add-new-address-inline {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1.5rem;
            border: 2px dashed #ddd;
            border-radius: 12px;
            color: var(--text-muted);
            text-decoration: none;
            transition: var(--transition);
            gap: 0.5rem;
            margin-top: 1rem;
        }

        .add-new-address-inline:hover {
            border-color: var(--primary);
            color: var(--primary);
            background: var(--bg-light);
        }

        @media (max-width: 992px) {
            .checkout-container {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>

<body>
    <!-- Header -->
    <%- include('../partials/header.ejs')%>

        <main>
            <div class="container">
                <h1 style="margin: 2rem 0; font-family: 'Fredoka', sans-serif; color: var(--primary);">Checkout</h1>

                <div class="checkout-container">
                    <div class="checkout-main">
                        <!-- Shipping Address -->
                        <div class="checkout-section">
                            <div class="checkout-title">
                                <div class="checkout-title-text">
                                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                                        stroke-width="2">
                                        <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path>
                                        <circle cx="12" cy="10" r="3"></circle>
                                    </svg>
                                    Shipping Address
                                </div>
                                <a href="/user/addresses" class="btn-link"
                                    style="font-size: 0.9rem; color: var(--accent);">Manage Addresses</a>
                            </div>

                            <div class="address-selection-grid">
                                <% if (addresses && addresses.length> 0) { %>
                                    <% addresses.forEach((address, index)=> { %>
                                        <label class="address-select-card <%= address.isDefault ? 'selected' : '' %>">
                                            <input type="radio" name="selectedAddress" value="<%= address._id %>"
                                                <%=address.isDefault ? 'checked' : '' %>
                                            onchange="this.closest('.address-selection-grid').querySelectorAll('.address-select-card').forEach(c
                                            => c.classList.remove('selected'));
                                            this.closest('.address-select-card').classList.add('selected');">
                                            <div class="address-info">
                                                <span class="address-badge">
                                                    <%= address.addressName %>
                                                </span>
                                                <strong>
                                                    <%= address.name %>
                                                </strong>
                                                <p>
                                                    <%= address.street %>
                                                </p>
                                                <p>
                                                    <%= address.city %>, <%= address.pincode %>
                                                </p>
                                                <p>Phone: <%= address.phone %>
                                                </p>
                                            </div>
                                        </label>
                                        <% }) %>
                                            <% } else { %>
                                                <p style="text-align: center; color: var(--text-muted); padding: 2rem;">
                                                    No addresses found. Please add a shipping address.</p>
                                                <% } %>

                                                    <a href="/user/addresses" class="add-new-address-inline">
                                                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none"
                                                            stroke="currentColor" stroke-width="2">
                                                            <line x1="12" y1="5" x2="12" y2="19"></line>
                                                            <line x1="5" y1="12" x2="19" y2="12"></line>
                                                        </svg>
                                                        Add New Address
                                                    </a>
                            </div>
                        </div>

                        <!-- Payment Method -->
                        <div class="checkout-section">
                            <h2 class="checkout-title">
                                <div class="checkout-title-text">
                                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                                        stroke-width="2">
                                        <rect x="1" y="4" width="22" height="16" rx="2" ry="2"></rect>
                                        <line x1="1" y1="10" x2="23" y2="10"></line>
                                    </svg>
                                    Payment Method
                                </div>
                            </h2>

                            <div class="payment-options">
                                <label class="payment-option">
                                    <input type="radio" name="payment" value="cod" checked>
                                    <span>Cash on Delivery (COD)</span>
                                </label>
                                <label class="payment-option">
                                    <input type="radio" name="payment" value="online">
                                    <span>Online Payment (Razorpay)</span>
                                </label>
                                <label class="payment-option">
                                    <input type="radio" name="payment" value="wallet">
                                    <span>Wallet</span>
                                </label>
                            </div>
                        </div>
                    </div>

                    <div class="checkout-sidebar">
                        <div class="order-summary-card">
                            <h2 class="checkout-title">Order Summary</h2>

                            <div class="summary-item">
                                <span>Subtotal</span>
                                <span>₹0.00</span>
                            </div>
                            <div class="summary-item">
                                <span>Shipping</span>
                                <span>Free</span>
                            </div>
                            <div class="summary-item">
                                <span>Tax</span>
                                <span>₹0.00</span>
                            </div>

                            <div class="summary-total">
                                <span>Total</span>
                                <span>₹0.00</span>
                            </div>

                            <button class="btn btn-primary place-order-btn">
                                Place Order
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </main>

        <!-- Footer -->
        <%- include('../partials/footer') %>
            <script src="/js/formValidator.js"></script>
</body>

</html>